# RA-Jiraf

<img width="800" height="100" alt="banner" src="https://github.com/user-attachments/assets/9b02744b-82e8-4323-9e0b-a7c89ccd9cf7" />

Краткий гайд по запуску проекта локально.

## Стек
- **PHP** (страницы + API)
- **MySQL** (услуги, заявки, контент)
- **Vue 3** (через CDN, без сборщика)

## Быстрый старт (XAMPP)
1. Установите XAMPP и запустите `Apache` + `MySQL`.
2. Скопируйте проект в папку `htdocs`, например:
   `C:\xampp\htdocs\RA-Jiraf`
3. Импортируйте структуру БД:
   в phpMyAdmin выполните SQL из файла `db/schema.sql`.
4. Откройте сайт в браузере:
   - Главная: `http://localhost/RA-Jiraf/`
   - Услуги: `http://localhost/RA-Jiraf/services.php`
   - Админка (только по прямой ссылке): `http://localhost/RA-Jiraf/admin.php`

ТЕКУЩИЙ ЛОГИН:ПАРОЛЬ: `admin:123`


## Новые бизнес-модули
- **Клиенты и личный кабинет**: клиент открывает `cabinet.php` по телефону, видит свои заявки, заказы, статусы и прикреплённые макеты.
- **Заказы и CRM-пайплайн**: менеджер в `admin.php` создаёт заказ из заявки, меняет статус заказа и ведёт историю обработки.
- **Калькулятор стоимости**: на странице `services.php#calculator` клиент выбирает услугу, тираж, площадь и параметры; базовые цены и параметры редактируются в админке.
- **Файлы макетов**: клиент или менеджер может прикрепить PDF/JPG/PNG/WEBP/SVG/ZIP к заявке или заказу.

Дополнительные таблицы создаются автоматически при входе в админку или обращении к API, но для чистой установки также можно импортировать полный `db/schema.sql`.
