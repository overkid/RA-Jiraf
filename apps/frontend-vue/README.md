# frontend-vue

Vue frontend for RA-Jiraf.

## Run

```bash
npm install
npm run dev
```

By default frontend expects API at `http://localhost:8000`.
You can override via env:

```bash
VITE_API_BASE=http://localhost:8000
```
